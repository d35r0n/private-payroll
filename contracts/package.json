{
  "name": "private-payroll-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "compile": "if command -v compact >/dev/null 2>&1; then compact compile payroll_contract.compact managed/payroll; else echo 'compact compiler not found, using pre-compiled managed contract artifacts'; fi",
    "build": "rm -rf dist && tsc --project tsconfig.build.json && cp -Rf ./managed ./dist/managed",
    "test": "vitest run"
  }
}
